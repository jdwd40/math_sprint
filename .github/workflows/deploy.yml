name: Deploy Vite App

on:
  push:
    branches:
      - master  # Replace with your main deployment branch if different

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'  # Use the Node.js version your app requires

    - name: Install dependencies
      run: npm install

    - name: Build Vite app
      run: npm run build

    - name: Deploy to VPS
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
        HOST: ${{ secrets.VPS_HOST }}
        USER: ${{ secrets.VPS_USER }}
        PORT: ${{ secrets.VPS_PORT }}
        TARGET_DIR: /var/www/jdwd40.com/html/ms

      run: |
        # Set up SSH keys and known_hosts for secure access
        mkdir -p ~/.ssh
        echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        echo "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts

        # Package and transfer files
        tar -czf dist.tar.gz -C dist .
        scp -P $PORT dist.tar.gz $USER@$HOST:$TARGET_DIR
        ssh -p $PORT $USER@$HOST "tar -xzf $TARGET_DIR/dist.tar.gz -C $TARGET_DIR && rm $TARGET_DIR/dist.tar.gz"
